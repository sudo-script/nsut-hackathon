{
  "name": "nsut-hackathon-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm ci --prefix web --include=dev && npm run build --prefix web",
    "start": "node server.mjs"
  }
}
